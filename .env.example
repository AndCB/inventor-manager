# Copy this file to `.env` and adjust as needed.
# Docker Compose reads `.env` automatically (docker compose up -d).

# SQL Server SA password. If you change this, update the connection string in
# backend/appsettings.json to match (Server=localhost,1433;...;Password=<same>).
MSSQL_SA_PASSWORD=Inventory!Dev123
